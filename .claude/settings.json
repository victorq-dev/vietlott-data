{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/user/Desktop/projects/personal/vietlott-data && uv run ruff check --select I --fix ./src 2>/dev/null && uv run ruff format ./src 2>/dev/null; exit 0",
            "description": "Auto-format Python files with ruff after edit"
          }
        ],
        "id": "post:edit-write:ruff-format"
      }
    ]
  }
}
